<?xml version="1.0" encoding="UTF-8"?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">    
<mapper namespace="io.renren.modules.sys.dao.SysConfigDao">

	<!-- 可根据需要自定义是否启用映射配置 -->
	<update id="updateValueByKey" parameterType="map">
		update sys_config set param_value = #{paramValue} where param_key = #{paramKey}
	</update>

	<!-- 可根据需要自定义是否启用映射配置 -->
	<select id="queryByKey" parameterType="string" resultType="string">
		select param_value from sys_config where param_key = #{paramKey}
	</select>

</mapper>
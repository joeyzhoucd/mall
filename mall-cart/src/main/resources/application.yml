server:
  port: 16000
spring:
  application:
    name: mall-cart
  redis:
    host: ${REDIS_HOST:192.168.77.100}
    port: ${REDIS_PORT:6379}
  thymeleaf:
    cache: false

mall:
  session:
    enabled: true

